# Work Command agent portraits

50 unchanged transparent PNGs from the user-supplied profile-picture package. See `profiles.json` for stable character IDs, 120 suggested role mappings, source hashes, and the approved Second in Command naming. Shared artwork is stored only once.

Implementation and sizing: [Agent identity guide](../../docs/AGENT-IDENTITY.md).
